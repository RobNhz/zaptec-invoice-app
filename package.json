{
  "name": "zaptec-invoice-app",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "postinstall": "npm --prefix frontend install",
    "build": "npm --prefix frontend run build",
    "dev": "npm --prefix frontend run dev"
  },
  "devDependencies": {
    "supabase": "^2.76.14"
  }
}
